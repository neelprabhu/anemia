% BME 260 Spring 2017
% Modeling Blood Flow in Healthy and Anemic Physiology

function b = mix(bHeart, bMus, bKid, bBone, bInt, bElse)

dist  = bHeart.dist;
CO    = bHeart.cOut;

totO2   = bHeart.co2 .* dist(1) + bMus.co2 .* dist(2) + bKid.co2 .* dist(3) + 
totCO2  =
totH2O  =
totGluc = 









end
